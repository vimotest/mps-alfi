activity Foo ( ) {
//  let a : Integer = 0;
//  WriteLine(IntegerFunctions::ToString(a));
  end = false;
  while(!end) {
    a = 3;
    b = 2;
    WriteLine(IntegerFunctions::ToString(a));
    WriteLine(IntegerFunctions::ToString(b));
    end = true;
  }
  WriteLine(IntegerFunctions::ToString((Integer)a));
}
